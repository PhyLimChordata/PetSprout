const cron = require('node-cron');
/*
 * Create a particular habit and return back the newly created habit.
 */

var jobs = {};

// Adds a new alarm, and returns an id
const add = async (alarm, id) => {
    return
}

// Stops a current cron job, given an id
const remove = async (id) => {
    return
}

// // horrible id implementation (pls fix)
// const getnewID = async () => {
//     i = 0;
//     while(typeof jobs[i] != "undefined") {

//     }
// }

exports.add = add;
exports.add = remove;